# dependencies
node_modules/

# build output
dist/
build/
coverage/
.cache/

# local env files
.env
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# IDE/editor directories
.vscode/
.idea/

# macOS
.DS_Store

# misc
*.local

# Google Apps Script - pasted into the Sheet's script editor, not part of the
# build. Ignored per request. NOTE: this means it is NOT version-controlled -
# an earlier copy was lost this way. Keep a backup outside the repo (or inside
# the Apps Script editor itself, which retains version history).
google-apps-script/Code.gs
